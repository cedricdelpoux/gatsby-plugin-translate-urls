const {getPathLocale} = require("./utils/get-path-locale")
const {TranslateUrlsContext} = require("./utils/translate-urls-context")
const {TranslatedLink} = require("./utils/translated-link")

module.exports = {
  getPathLocale,
  TranslateUrlsContext,
  TranslatedLink,
}
